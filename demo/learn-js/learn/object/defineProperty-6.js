'use strict';
var project = {};

Object.defineProperty( project, 'securityCode'
                              , { value: "123-65-6879", enumerable: true } );

console.log(project);

console.log( Object.keys(project) );