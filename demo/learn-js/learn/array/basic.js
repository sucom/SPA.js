/* 
 * Array: Reference Type
 * 
 * can hold any dataTypes.
 * 
 * var nObj = [1, 2, 3, 4];
 * 
 * var aObj = [1, 'a', 'b', -3];
 * 
 * var mObj = [1, 'a', true, {}];
 * 
 * var cObj = [,,,,,,,];
 * 
 * var xObj = []; // xObj[100] is an valid access
 * 
 */